[project]
name = "harxitflow-sdk"
version = "0.1.1"
description = "Python SDK for the HarxitFlow REST API"
readme = "README.md"
requires-python = ">=3.10,<3.16"
license = "MIT"
keywords = ["harxitflow", "sdk", "ai", "workflow"]

dependencies = [
    "httpx[http2]>=0.24.0,<1.0.0",
    "pydantic>=2.0.0,<3.0.0",
    "pydantic-settings>=2.10.1,<3.0.0",
    "tomli>=2.2.1,<3.0.0",
    "typing-extensions>=4.14.0,<5.0.0",
]

[project.optional-dependencies]
testing = [
    "pytest>=8.0",
    "pytest-asyncio>=0.26.0",
]

[project.entry-points."pytest11"]
harxitflow = "harxitflow_sdk.testing"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/harxitflow_sdk"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
python_files = "test_*.py"
python_classes = "Test*"
python_functions = "test_*"
addopts = "-v --tb=short --strict-markers --disable-warnings --color=yes"
markers = [
    "unit: Unit tests",
    "integration: Integration tests (require a live HarxitFlow instance)",
]

[dependency-groups]
dev = [
    "pytest>=8.4.1",
    "pytest-asyncio>=0.26.0",
    "pytest-cov>=7.0.0",
    "respx>=0.21.0",
    "ruff>=0.9.10",
]
