# HarxitFlow Service Configuration
#
# This file tells LFX to use HarxitFlow's full-featured service implementations
# instead of the minimal/noop services built into LFX.
#
# Usage:
#   1. Copy this file to your HARXITFLOW_CONFIG_DIR as lfx.toml
#   2. Or add to pyproject.toml under [tool.lfx.services]
#
# The services will be automatically discovered and loaded when you run:
#   - harxitflow run
#   - lfx serve (when run from harxitflow project)

[services]
# Core Services - Full-featured HarxitFlow implementations
database_service = "harxitflow.services.database.service:DatabaseService"
storage_service = "harxitflow.services.storage.local:LocalStorageService"
cache_service = "harxitflow.services.cache.service:ThreadingInMemoryCache"

# Authentication & Session Management
auth_service = "harxitflow.services.auth.service:AuthService"
session_service = "harxitflow.services.session.service:SessionService"

# Chat & Communication
chat_service = "harxitflow.services.chat.service:ChatService"

# State & Data Management
state_service = "harxitflow.services.state.service:StateService"
variable_service = "harxitflow.services.variable.service:VariableService"

# Background Processing
task_service = "harxitflow.services.task.service:TaskService"
job_queue_service = "harxitflow.services.job_queue.service:JobQueueService"

# Marketplace & Components
store_service = "harxitflow.services.store.service:StoreService"
shared_component_cache_service = "harxitflow.services.shared_component_cache.service:SharedComponentCacheService"

# Observability
telemetry_service = "harxitflow.services.telemetry.service:TelemetryService"
tracing_service = "harxitflow.services.tracing.service:TracingService"

# Note: settings_service is NOT configurable - it always uses the built-in implementation
# Note: mcp_composer_service uses the lfx implementation by default
